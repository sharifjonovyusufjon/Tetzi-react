import { Box, Container } from "@mui/material";

export default function OtherContact() {
  return (
    <div className="other-contact">
      <Container className="other-contact-container">
        <Box className={"contact-title"}>CONTACTS</Box>
        <Box className={"contact-text"}>Home / Contacts</Box>
      </Container>
    </div>
  );
}
