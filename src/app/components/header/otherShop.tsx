import { Box, Container } from "@mui/material";

export default function OtherShop() {
  return (
    <div className="other-shop">
      <Container className="other-shop-container">
        <Box className={"shop-title"}>PRODUCTS</Box>
        <Box className={"shop-text"}>Home / Products</Box>
      </Container>
    </div>
  );
}
